# Net Dust 1.0
> 2022-01-21 released
> Revit(formerly ribbit) based

> commands

`code start` // All code must start with code start.
`print()` 
`revit reply` // Existed because Net Dust was revit based.
`recognize`
`import` // Bring information
`define`
`code{}`
etc.
